/**
 * 비전 페이지 (/vision)
 *
 * 로그인 여부와 관계없이 서비스 비전 콘텐츠 표시.
 */

import { LandingSection } from "@/components/landing/LandingSection";

export default function VisionPage() {
  return <LandingSection />;
}
